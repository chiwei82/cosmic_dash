class GameView {
    constructor(state) {
        this.state = state;
    }

    draw() {
        // Draw everything to the canvas
    }

    // drawHUD() { // Display HUD with score }
    // drawGameOver()
}